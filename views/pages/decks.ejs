<%- include("../partials/header"); %>

<a href="/subjects">subjects</a> / <a href="/subject-view/<%= city.subject_id %>"><%= city.s_name %></a>

<h1><%= city.name %> - Available Decks </h1>

<a href="/create-deck/<%= city.course_id %>">Add new deck</a>

<div class="main-cards">
    <% for (const row of res) { %>
        <div class="card-preview">
            <p>
                <a href="/specific-deck/<%= row.deck_id%>"><%= row.name %></a>
            </p>
        </div>
    <% }; %>
</div>

<%- include("../partials/footer"); %>
